# Результаты локальной проверки

Дата: **21 сентября 2026**, macOS arm64. Go 1.24.4, Node.js 24.15.0, PostgreSQL 17 в Podman. Браузер: Chromium 153.0.8010.12 (Playwright). Тестовая и рабочая базы разделены.

## Проверка после рефакторинга бэкенда — 21.09.2026

- `make test`: совпали все 21 API-маршрут с OpenAPI; Go-тесты, `go vet`, TypeScript и production-сборка React прошли.
- Модульные проверки: 22 функции `Test...` (19 в `internal/forum`, 3 в `cmd/api`), включая табличные подслучаи. До рефакторинга было 5 функций модульных тестов.
- `make integration`: прошли 10 прежних сценариев `TestIntegrationForum` и 2 новых сценария `TestIntegrationModerationTransactions` с реальной PostgreSQL и `-race`. Пакет `internal/forum` вместе с unit-тестами завершился за 8,172 с; гонок данных не обнаружено.
- Новые интеграционные сценарии проверяют конкурирующие hide/dismiss одной жалобы (одно решение, один согласованный audit) и полный откат решения/состояния темы при отказе записи журнала.
- `make e2e`: все 6 сценариев Chromium прошли за 4,2 с. Изменения React не потребовались.
- Сохранены формат прежних хешей идемпотентности, JSON/status/header сохранённых ответов и `429` с `Retry-After`; это закреплено регрессионными тестами.

Ожидаемые ERROR-записи в новых unit-тестах проверяют внутреннюю ошибку и recovery после panic; в интеграции по-прежнему намеренно отклоняется запись поста для проверки rollback. Эти записи не означают падения тестов.

Для проверки использовались только `forum_test` и `forum_e2e_test`. Временный сервер на 18080 остановлен. Уже работавшие перед рефакторингом сервер на 8080 и контейнер PostgreSQL сохранены; для применения новой версии к этому серверу нужен `make stop`, затем `make start`.

Структура и границы транзакций описаны в [BACKEND.md](BACKEND.md). Проверки эксплуатационных NFR, перечисленные ниже, по-прежнему требуют отдельного стенда.

## Первоначальная проверка приложения

| Проверка | Результат |
|---|---|
| Unit-тесты: Unicode-границы, Argon2id/соль, лимит, случайный токен/CSRF, атрибуты cookie | 5 тестов пройдено |
| Интеграция с PostgreSQL + Go race detector | 10 сценариев пройдено, race не обнаружен; весь набор ~7,9 с |
| Публикация → ответ → удаление → очищенный черновик | Playwright: пройдено |
| Потеря ответа после записи на сервере → перезагрузка вкладки → повтор без дубля | Playwright: пройдено |
| Жалоба → отдельный вход администратора → скрытие → журнал → публичный текст недоступен | Playwright: пройдено |
| Создание и архивирование раздела через интерфейс | Playwright: пройдено |
| Пользовательский HTML остаётся текстом | Playwright: пройдено |
| 360 / 768 / 1440 px, меню, форма, отсутствие горизонтального переполнения, клавиатурный переход к содержимому | Playwright: пройдено |
| Все 6 браузерных сценариев | Пройдено за ~4 с |
| OpenAPI ↔ зарегистрированные HTTP-маршруты | 21 маршрут совпадает |
| Резервная копия pg_dump → pg_restore в отдельную базу | Восстановлены 5 разделов, 5 тем, 10 сообщений |

Интеграционные сценарии проверяют: валидацию и Origin/CSRF; идемпотентность и ограничение частоты; права владельца и отсутствие внутренних полей в публичном DTO; одновременные повторы ответа; жалобы/скрытие/поиск/аудит; закрытие и открытие темы; роли и архив; удаление и отклонение повторно предъявленного отозванного токена; откат транзакции при искусственном отказе записи первого сообщения; пагинацию 20+5 без повторов и поисковый запрос с SQL-метасимволами.

В сценарии отката намеренно создаётся ошибка PostgreSQL. Единственная ожидаемая запись ERROR содержит request_id и тип ошибки — без текста запроса и пользовательского сообщения. Это часть проверки атомарности, а не сбой итогового прогона.

Полный воспроизводимый набор: `make test`, `make integration`, `make e2e`. Последний HTML-отчёт браузерных тестов: `frontend/playwright-report/index.html`.

## Что ещё требуется для полной приёмки NFR из отчётов

- **TC-17:** отдельный 15-минутный нагрузочный прогон на согласованном стенде 2 vCPU / 4 ГБ, с 10 тыс. тем и 50 тыс. сообщений. Скрипт и SQL-фикстура подготовлены, целевые p95 не заявляются как достигнутые.
- **TC-18:** ручная проверка Firefox, полного управления с клавиатуры, экранного диктора, масштаба браузера 200% и замера первого экрана при заданном ограничении сети. Проверка размеров и клавиатурного перехода в Chromium выполнена.
- **TC-16:** внешняя HTTPS-публикация. Локальный стенд использует HTTP на loopback и `COOKIE_SECURE=false`; production-конфигурация требует HTTPS + Secure cookie. Проверка атрибутов выполнена модульным тестом, реальный TLS-стенд не развёрнут.
- **TC-20:** восстановление небольшой локальной копии проверено; RPO ≤ 24 ч и RTO ≤ 60 мин для эксплуатационного объёма требуют ежедневного расписания и отдельного хронометража.
- **TC-21:** потеря ответа и повтор проверены; доступность 99,5% за календарный месяц требует месячного наблюдения.

Для обновлённых DOCX 21.09.2026 получены снимки работающего интерфейса на отдельной временной БД. Дополнительно выполнены 19 успешных утверждений: восемь границ заголовка/текста, пагинация 25 постов и отсутствие скрытого/удалённого текста, поиск с фильтром и пустым результатом, три разрешённые жалобы и четвёртая с 429 / Retry-After. Результаты: `docs/evidence/supplemental-evidence.json`.

По каталогу отчёта: 17 из 22 TC пройдены полностью, TC-16/18/20/21 — частично, TC-17 не выполнен. Наборы автотестов и укрупнённые TC имеют разные единицы учёта. Стенд после получения снимков остановлен.
